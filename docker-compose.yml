version: '3.8'

services:
  hydrofabric:
    build:
      context: ./ngen-workflow/
      dockerfile: Dockerfile
    volumes:
      - ./ngen-workflow:/ngen-workflow
    working_dir: /ngen-workflow
    ulimits:
      stack: 67108864
